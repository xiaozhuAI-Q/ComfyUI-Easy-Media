#!/usr/bin/env sh
if git diff --cached --name-only | grep -q "^frontend/.*\.\(tsx\?|css\)$"; then
  echo "Frontend changes detected, rebuilding..."
  cd frontend && bun run build:release && cd ..
  git add frontend/dist/
  echo "Done."
fi
